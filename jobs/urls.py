from django.urls import path
from . import views




urlpatterns = [
    # Remove or comment out the following line if 'job_detail' view is not defined
    # path('<int:pk>/', views.job_detail, name='job_detail'),  
]
